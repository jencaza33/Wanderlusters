import { makeStyles } from "@material-ui/core";

export default makeStyles((theme) => ({
  container: {
    padding: "100px",
    marginLeft: "300px",
    marginRight: "300px",
    display: "flex",
    flexDirection: "column",
  },
}));
